export const POINTS_MESSAGE = 'Your points are ';
export const WIN = 'Win';
export const LOOSE = 'Loose';
export const WIN_POINTS = 100;
export const LOOSE_POINTS = 50;

// Defining routes
export const routes = {
  POINT_TRACKER: 'pointTracker',
};

// Defining screens title
export const screens = {
  POINT_TRACKER: 'Point Tracker',
};
